{% comment %}
  This page represents the /collections and /products pages.
{% endcomment %}

<div class="flex-container list-collections">
  {% for collection in collections %}
    {% unless collection.handle == 'frontpage' %}
      <div class="list-collections__collection">
        <div class="list-collections__collection__header">
          <h2><a href="{{ collection.url }}" title="{{ collections.general.link_title | t: title: title }}">{{ collection.title }}</a></h2>
          <a href="{{ collection.url }}" title="{{ collections.general.link_title | t: title: title }}" class="list-collections__collection__header__more">{{ 'collections.general.more' | t }} {{ collection.title }} &rsaquo;</a>
        </div>
        <div class="product-grid noflex-container">
          <div class="product-grid__products">
            {% for product in collection.products limit: 4 %}
              <div class="product-grid__single">
                <a href="{{ product.url }}" class="product-grid__image">
                  <img src="{{ product.featured_image.src | img_url: '480x480' }}" alt="{{ product.featured_image.alt | escape }}">
                </a>
                <a href="{{ product.url }}" class="product-grid__title">{{ product.title }}</a>
                <p class="product-grid__price">{{ product.price | money }}</p>
              </div>
            {% endfor %}
          </div>
        </div>
      </div>
    {% endunless %}
  {% endfor %}
</div>
